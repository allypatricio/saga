<div class="play-box">
<div class="play-error">
  <% flash.each do |key, value| %>
      <div class="alert alert-<%= key %>"><%= value %></div>
  <% end %>
</div>
<p>(3/3)</p>
<h4> Story </h4>
  <p>
    Upstairs, you can see the inner workings of the brewery.
  </p>
<h4> Question </h4>
  <p>
    How many tanks of liquid gold can you see?
    <form class="form-inline" action="/play/final" method="get">
      <input type="text" name="answer" class="form-control">
      <input type="submit" value="Submit" class="btn btn-primary">
    </form>
  </p>
</div>
